{
  "name": "@gcp-tools/services-scheduler-api",
  "author": "developers@gcp-tools.com",
  "description": "pragma | services | scheduler | api",
  "license": "Apache-2.0",
  "type": "module",
  "version": "0.0.0",
  "main": "./dist/handler.mjs",
  "scripts": {
    "build": "tsc --pretty",
    "clean": "rm -rf coverage dist",
    "lint": "biome check src --write",
    "postbuild": "node postbuild.mjs",
    "test": "jest --config jest.config.mjs --passWithNoTests",
    "test:watch": "jest --watch --config jest.config.mjs --passWithNoTests"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^3.4.0"
  },
  "devDependencies": {
    "@biomejs/biome": "1.8.1"
  },
  "engines": {
    "node": ">=20.12.2",
    "npm": ">=10.5.0"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://tbc"
  }
}
